<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.company.mvc.emp.EmpMapper">
	<select id="getEmp" parameterType="EmpVO" resultType="Map">
		SELECT *
			FROM employees
			WHERE employee_id = #{employeeId}
	</select>
	<select id="getEmpList" parameterType="EmpVO" resultType="EmpVO">
		SELECT *
			FROM employees
			<where>
				<if test="departmentId != null">
					department_id = #{departmentId}
				</if>
			</where>
			ORDER BY 1
	</select>
	<select id="getEmpByDept" resultType="EmpVO">
		SELECT *
			FROM employees
			<where>
				<foreach item="item" collection="array" open="department_id IN (" separator="," close=")">
					#{item}
				</foreach>
			</where>
	</select>
</mapper>
